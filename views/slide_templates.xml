<odoo>
  <template id="matched_slides_extension" inherit_id="website_slides.slide_channels">
    <!-- Insert after the course cards container -->
    <xpath expr="//div[@id='o_wslides_channel_main']" position="after">
      <!-- Display matched slides if any -->
      <t t-if="matched_slides">
        <section class="container my-5">
          <h4>Matched Content</h4>
          <div class="row">
            <t t-foreach="matched_slides" t-as="slide">
              <div class="col-md-4 mb-3">
                <div class="card h-100 shadow-sm">
                  <div class="card-body">
                    <h5 class="card-title" t-esc="slide.name"/>
                    <p class="card-text" t-esc="slide.description"/>
                    <a t-att-href="'/slides/%s' % slide.channel_id.id"
                       class="btn btn-sm btn-outline-primary mt-2">
                      View in Course
                    </a>
                  </div>
                </div>
              </div>
            </t>
          </div>
        </section>
      </t>
    </xpath>
  </template>
</odoo>
